<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="./styles.css">
</head>

<body>

    <section class="wrapper">
        <form class="inputArea" action="/addItem" method="post">
            <input class="input" name="name" type="text" />
            <button class="button" type="submit">Add todo</button>
        </form>

        <div class="container">
            <div class="incomplete">
                <h1>Incomplete</h1>
                {{#todos}}
                <div class="returnItem">
                    <form action="{{id}}" method="post">
                        <button class="button" type="submit" name="removeItem" value="{{id}}"> Mark Complete </button>
                        <input class="todoItem" type="text" value="{{name}}" name="name" placeholder="{{name}}" />
                        <button class="button" type="submit" formaction="/todo/edit/{{id}}" name="editItem" value="{{id}}"> Edit </button>
                        <button class="button" type="submit" formaction="/todo/delete/{{id}}" name="editItem" value="{{id}}"> Delete </button>
                    </form>
                </div>
                {{/todos}}
            </div>


            <div class="completed">
                <h1>Completed</h1> <span><form action="/todo/clear" method ="POST"><span><button class ="clearButton" type="submit">Clear Completed</button></span></form>
                </span>
                {{#complete}}
                <div class="complete">
                    {{name}}
                </div>
                {{/complete}}
            </div>


        </div>



    </section>

</body>

</html>